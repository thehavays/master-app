package com.vestel.tv.middleware;

/**
 * @brief Defines the backlight modes that are displayed under the picture settings of Android device
 */
@SuppressWarnings("unused")
public enum VestelBacklightMode
{
    BACKLIGHT_MODE_INVALID,
    BACKLIGHT_MODE_OFF,
    BACKLIGHT_MODE_MIN,
    BACKLIGHT_MODE_MED,
    BACKLIGHT_MODE_ECO
}

