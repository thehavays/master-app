package com.vestel.tv.middleware;

import android.content.Context;

/**
 * @brief This class manages screensaver feature of Androoid device
 */
public class VestelDreamManager
{
    private static final String TAG = "VestelDreamManager";


    /**
     * @brief Initializes the class with the given Android application context
     * @param context Android application context
     */
    public static void init(Context context)
    {
    }

    /**
     * @brief Activates the screensaver that the user has set in settings menu
     * @note This function does nothing if the user has set the screensaver as "none"
     */
    public static void startDreaming()
    {
    }
}
